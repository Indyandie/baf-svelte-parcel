{
  "name": "svelte-test",
  "version": "1.0.0",
  "description": "basic af svelte and parcel",
  "scripts": {
    "start": "parcel src/index.html --no-cache",
    "server": "npm run build && serve -s build",
    "build": "rm -rf build && parcel build --no-source-maps src/index.html -d dist"
  },
  "keywords": [
    "svelte",
    "parcel",
    "basic"
  ],
  "author": "Andy Contreras",
  "license": "ISC",
  "dependencies": {
    "svelte": "^3.28.0"
  },
  "devDependencies": {
    "@babel/polyfill": "^7.11.5",
    "less": "^3.12.2",
    "parcel": "^2.0.0-nightly.410",
    "parcel-transformer-svelte": "^1.0.5",
    "serve": "^11.3.2"
  }
}
